import React from 'react';
import {View, Text, Image, StyleSheet, Dimensions} from 'react-native';

const PassbookView = (props) => {
  return(	
	  <Text>Welcome to the Passbook Screen</Text>
  );		
} 

export default PassbookView;